# 出力フォーマット詳細

再検証完了後の最終結果のみを以下の形式で出力する。

## セクション省略ルール

- **0 件の重大度セクションは省略する**（例: Critical が 0 件なら「🔴 Critical Issues」セクション自体を出力しない）
- **実行しなかったステップに対応するセクションは省略する**（例: CI/CD のみの変更で Step 2〜5・Step 7 を実行していない場合、「アーキテクチャ整合性」「テストカバレッジ」は出力しない）

## テンプレート

````markdown
## レビュー結果サマリー

### 対象
- ブランチ: {branch_name}
- 変更ファイル数: {n} files
- 変更行数: +{added} / -{removed}

### 重大度別の問題数
| 重大度 | 件数 |
|--------|------|
| 🔴 Critical | {n} |
| 🟠 Major | {n} |
| 🟡 Minor | {n} |
| 💡 Suggestion | {n} |

---

## 🔴 Critical Issues（必須修正）

### 1. [問題のタイトル]

**ファイル**: `path/to/file.py:行番号`

**問題**: 問題の詳細説明

**現在のコード**:
```python
# 問題のあるコード
```

**修正例**:
```python
# 修正後のコード
```

**理由**: なぜこの修正が必要か

---

## 🟠 Major Issues（要修正）
（同様のフォーマット）

## 🟡 Minor Issues（推奨修正）
（同様のフォーマット）

## 💡 Suggestions（提案）
（同様のフォーマット）

---

## アーキテクチャ整合性

| チェック項目 | 結果 |
|-------------|------|
| 依存方向 | ✅/⚠️/❌ |
| ファイル配置 | ✅/⚠️/❌ |
| Protocol 整合性 | ✅/⚠️/❌ |
| DI コンテナ登録 | ✅/⚠️/❌ |
| simulation モジュール同期 | ✅/⚠️/❌ / ⏭ 該当変更なし |

---

## 影響範囲の確認結果

### 修正が必要な関連ファイル
- `path/to/related-file.py`: 理由

### 確認済み（問題なし）
- `path/to/checked-file.py`: 確認内容

---

## モノレポ横断影響分析

### 変更ファイルの影響スコープ

| 変更ファイル | 影響スコープ | 判定根拠 |
|------------|------------|---------|
| `{変更ファイルパス}` | bid_allocation_agent のみ / 全パッケージ | {paths フィルタの有無、参照関係等} |

### パッケージ別影響判定

| パッケージ | 影響 | 検証内容 | 根拠 |
|-----------|------|---------|------|
| sns_structure_agent | ✅ 影響なし / ⚠️ 要確認 / ❌ 影響あり | {実施した検証内容} | {影響なしと判断した具体的理由} |
| cr_agent | ✅ 影響なし / ⚠️ 要確認 / ❌ 影響あり | {実施した検証内容} | {影響なしと判断した具体的理由} |

### 影響ありの場合の詳細（該当時のみ）
- **対象パッケージ**: {パッケージ名}
- **影響内容**: {具体的にどのような影響が発生するか}
- **推奨対応**: {修正案または確認すべき事項}

---

## テストカバレッジ

| カテゴリ | 網羅状況 |
|---------|---------|
| 正常系 | ✅/⚠️/❌ |
| 異常系 | ✅/⚠️/❌ |
| 境界値 | ✅/⚠️/❌ |
| テーブル駆動準拠 | ✅/⚠️/❌ |

### 不足しているテストケース
1. {テストケースの説明}

---

## Codex 補完レビュー

**実行ステータス**: ✅ 実行 / ⏭ スキップ（理由: codex CLI 未インストール / レビュー対象がすべて `.claude/**` 配下のため除外後に空 / タイムアウト / 実行エラー / 空応答）

### (A) Claude が見落とした指摘
- [重大度] file.py:行 — 内容（Claude が見落とした理由・Codex の根拠）

### (B) Claude の指摘への反対意見
- [重大度] file.py:行 — Claude の指摘要約 / Codex の反論

### (C) Claude の指摘を補強する観点
- [重大度] file.py:行 — 内容

### Claude による Codex 指摘の最終判定

| Codex 指摘 | Claude の判定 | 採否 | 理由 |
|-----------|------------|------|------|
| {Codex の指摘要約} | 妥当 / 不採用 / 要追加調査 | ✅/❌/⚠️ | {判定理由} |

---

## 総評
全体的な評価とアドバイス
````
